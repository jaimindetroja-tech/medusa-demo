# 🗺️ Complete Function Call Flow with File Locations

## 📍 Entry Point: Scheduled Job Execution

```
┌─────────────────────────────────────────────────────────────────┐
│  MEDUSA FRAMEWORK (Cron Scheduler)                              │
│  Triggers at: 0 0 * * * (Daily at Midnight)                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  export default async function syncProductsJob(container) │  │
│  │  Line: 153                                                │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
```

---

## 🔄 Complete Execution Flow

### PHASE 1: Fetch All Products

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 160                                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  const externalProducts = await fetchAllProducts()              │
│                                    └──────┬──────────┘          │
└───────────────────────────────────────────┼─────────────────────┘
                                            │
                                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: fetchAllProducts()                                   │
│  Line: 64-96                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  while (hasMore) {                                              │
│    const url = `${DUMMYJSON_API_URL}?limit=${PAGE_SIZE}&skip=${skip}` │
│                                                                 │
│    const response = await fetchWithRetry<DummyJSONResponse>(url) │
│                           └──────┬──────────┘                   │
└───────────────────────────────────┼─────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/lib/retry.ts                                            │
│  Function: fetchWithRetry<T>()                                  │
│  Line: 26-70                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  for (let attempt = 0; attempt <= config.maxRetries; attempt++) │
│    try {                                                        │
│      const response = await fetch(url)  ← Native fetch API     │
│      return await response.json() as T                          │
│    } catch (error) {                                            │
│      // Calculate exponential backoff delay                     │
│      await new Promise((resolve) => setTimeout(resolve, delay)) │
│    }                                                            │
│                                                                 │
│  Returns: DummyJSONResponse { products: [...], total: 194 }    │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    │ (Returns to fetchAllProducts)
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: fetchAllProducts()                                   │
│  Line: 80-87                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  allProducts.push(...response.products)                         │
│  skip += PAGE_SIZE                                              │
│  hasMore = allProducts.length < response.total                  │
│                                                                 │
│  Returns: DummyJSONProduct[] (194 products)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 2: Sync Categories

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 175-200                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  // Extract unique categories                                   │
│  const uniqueCategories = new Set(externalProducts.map(p => p.category)) │
│                                                                 │
│  // Transform to input format                                   │
│  const categoryInputs = Array.from(uniqueCategories).map(...)   │
│                                                                 │
│  // Call workflow                                               │
│  const { result } = await syncCategoriesWorkflow(container).run({ │
│                           └──────┬──────────┘                   │
└───────────────────────────────────┼─────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/workflows/sync-categories.ts                            │
│  Workflow: syncCategoriesWorkflow                               │
│  Line: 103-109                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  export const syncCategoriesWorkflow = createWorkflow(          │
│    "sync-categories",                                           │
│    (input: { categories: CategoryInput[] }) => {                │
│      const result = syncCategoriesStep(input)                   │
│                     └──────┬──────────┘                         │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/workflows/sync-categories.ts                            │
│  Step: syncCategoriesStep                                       │
│  Line: 22-100                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  const query = container.resolve("query")                       │
│                                                                 │
│  // Query existing categories                                   │
│  const { data: existingCategories } = await query.graph({       │
│    entity: "product_category",                                  │
│    fields: ["id", "name", "handle"]                             │
│  })                                                             │
│                                                                 │
│  // Filter new categories                                       │
│  const categoriesToCreate = input.categories.filter(...)        │
│                                                                 │
│  // Create new categories using Medusa workflow                 │
│  if (categoriesToCreate.length > 0) {                           │
│    const { result } = await createProductCategoriesWorkflow(    │
│                             └──────┬──────────┘                 │
└───────────────────────────────────┼─────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│  📦 @medusajs/medusa/core-flows                                 │
│  Workflow: createProductCategoriesWorkflow                      │
│  (Built-in Medusa Workflow)                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Creates categories in database                                 │
│  Returns: Created category objects with IDs                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    │ (Returns to syncCategoriesStep)
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/workflows/sync-categories.ts                            │
│  Step: syncCategoriesStep                                       │
│  Line: 59-79                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  // Build category map (slug → id)                              │
│  const categoryMap: Record<string, string> = {}                 │
│  for (const cat of input.categories) {                          │
│    const id = existingCategoryMap.get(cat.slug)                 │
│    if (id) categoryMap[cat.slug] = id                           │
│  }                                                              │
│                                                                 │
│  Returns: {                                                     │
│    created: 24,                                                 │
│    existing: 0,                                                 │
│    categoryMap: { "smartphones": "cat_01...", ... }             │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 3: Check Existing Products

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 203-221                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  const query = container.resolve("query")                       │
│                                                                 │
│  const { data: existingProducts } = await query.graph({         │
│    entity: "product",                                           │
│    fields: ["id", "handle", "metadata"]                         │
│  })                                                             │
│                                                                 │
│  // Build maps for quick lookup                                 │
│  const existingProductMap = new Map(                            │
│    existingProducts                                             │
│      .filter(p => p.metadata?.external_id)                      │
│      .map(p => [p.metadata!.external_id, p])                    │
│  )                                                              │
│                                                                 │
│  const dbHandles = new Set(existingProducts.map(p => p.handle)) │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 4: Transform & Categorize Products

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 224-284                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  for (const externalProduct of externalProducts) {              │
│                                                                 │
│    const product = mapToMedusaFormat(externalProduct)           │
│                    └──────┬──────────┘                          │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: mapToMedusaFormat()                                  │
│  Line: 102-140                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  function mapToMedusaFormat(product: DummyJSONProduct) {        │
│                                                                 │
│    // Generate handle                                           │
│    const rawHandle = product.title                              │
│      .toLowerCase()                                             │
│      .replace(/[^a-z0-9]+/g, "-")                               │
│      .replace(/^-+|-+$/g, "")                                   │
│                                                                 │
│    return {                                                     │
│      title: product.title,                                      │
│      handle: rawHandle,                                         │
│      description: product.description,                          │
│      metadata: {                                                │
│        external_id: product.id.toString(),                      │
│        category: product.category,                              │
│      },                                                         │
│      thumbnail: product.thumbnail,                              │
│      images: product.images.map(url => ({ url })),              │
│      variants: [{                                               │
│        title: "Default",                                        │
│        prices: [{                                               │
│          amount: Math.round(product.price * 100), // To cents   │
│          currency_code: "usd"                                   │
│        }]                                                       │
│      }]                                                         │
│    }                                                            │
│  }                                                              │
│                                                                 │
│  Returns: MedusaProductInput                                    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ (Returns to loop)
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 243-283                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│    const externalId = externalProduct.id.toString()             │
│    const existing = existingProductMap.get(externalId)          │
│                                                                 │
│    if (existing) {                                              │
│      // Product exists → UPDATE                                 │
│      product.handle = existing.handle                           │
│      productsToUpdate.push({                                    │
│        id: existing.id,                                         │
│        data: { title, description, metadata, ... }              │
│      })                                                         │
│    } else {                                                     │
│      // New product → CREATE                                    │
│                                                                 │
│      // Ensure unique handle                                    │
│      let handle = product.handle                                │
│      while (usedHandles.has(handle) || dbHandles.has(handle)) { │
│        handle = `${originalHandle}-${counter++}`                │
│      }                                                          │
│      product.handle = handle                                    │
│                                                                 │
│      // Link to category                                        │
│      const categorySlug = externalProduct.category              │
│        .toLowerCase()                                           │
│        .replace(/[^a-z0-9]+/g, "-")                             │
│      if (categoryMap[categorySlug]) {                           │
│        product.category_ids = [categoryMap[categorySlug]]       │
│      }                                                          │
│                                                                 │
│      productsToCreate.push(product)                             │
│    }                                                            │
│  }                                                              │
│                                                                 │
│  Result:                                                        │
│  • productsToCreate: MedusaProductInput[]                       │
│  • productsToUpdate: { id, data }[]                             │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 5: Batch Processing - CREATE

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 300-320                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  if (productsToCreate.length > 0) {                             │
│                                                                 │
│    for await (const batch of batchGenerator(productsToCreate, BATCH_SIZE)) { │
│                               └──────┬──────────┘               │
└───────────────────────────────────────┼─────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/lib/batch-processor.ts                                  │
│  Function: batchGenerator<T>()                                  │
│  Line: 19-26                                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  export async function* batchGenerator<T>(                      │
│    items: T[],                                                  │
│    batchSize: number                                            │
│  ): AsyncGenerator<T[], void, unknown> {                        │
│                                                                 │
│    for (let i = 0; i < items.length; i += batchSize) {          │
│      yield items.slice(i, i + batchSize)                        │
│    }                                                            │
│  }                                                              │
│                                                                 │
│  Yields: Batches of 15 products at a time                       │
└─────────────────────────────────────────────────────────────────┘
                                        │
                                        │ (Yields to loop)
                                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 307-318                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│      try {                                                      │
│        await createProductsWorkflow(container).run({            │
│              └──────┬──────────┘                                │
└───────────────────────┼─────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📦 @medusajs/medusa/core-flows                                 │
│  Workflow: createProductsWorkflow                               │
│  (Built-in Medusa Workflow)                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Input: { products: MedusaProductInput[] } (15 products)        │
│                                                                 │
│  Steps:                                                         │
│  1. Validate product data                                       │
│  2. Create products in database                                 │
│  3. Create variants                                             │
│  4. Set prices                                                  │
│  5. Link to categories                                          │
│  6. Upload images (if applicable)                               │
│                                                                 │
│  Returns: Created product objects                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                        │
                        │ (Returns to loop)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 311-318                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│        createdCount += batch.length                             │
│        console.log(`✅ Batch ${batchNum}: Created ${batch.length} products`) │
│                                                                 │
│      } catch (error) {                                          │
│        errorCount += batch.length                               │
│        console.error(`❌ Batch ${batchNum} failed:`, error)     │
│        // Continue with next batch                              │
│      }                                                          │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 6: Batch Processing - UPDATE

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 324-345                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  if (productsToUpdate.length > 0) {                             │
│                                                                 │
│    for await (const batch of batchGenerator(productsToUpdate, BATCH_SIZE)) { │
│                               └──────┬──────────┘               │
│                                      │                          │
│                                      │ (Same batchGenerator)    │
│                                      ▼                          │
│                                                                 │
│      try {                                                      │
│        await updateProductsWorkflow(container).run({            │
│              └──────┬──────────┘                                │
└───────────────────────┼─────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📦 @medusajs/medusa/core-flows                                 │
│  Workflow: updateProductsWorkflow                               │
│  (Built-in Medusa Workflow)                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Input: { products: [{ id, data }] } (15 products)              │
│                                                                 │
│  Steps:                                                         │
│  1. Validate update data                                        │
│  2. Update products in database                                 │
│  3. Update variants (if applicable)                             │
│  4. Update prices (if applicable)                               │
│  5. Update images (if applicable)                               │
│                                                                 │
│  Returns: Updated product objects                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                        │
                        │ (Returns to loop)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 335-343                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│        updatedCount += batch.length                             │
│        console.log(`✅ Batch ${batchNum}: Updated ${batch.length} products`) │
│                                                                 │
│      } catch (error) {                                          │
│        errorCount += batch.length                               │
│        console.error(`❌ Batch ${batchNum} failed:`, error)     │
│      }                                                          │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

### PHASE 7: Final Summary

```
┌─────────────────────────────────────────────────────────────────┐
│  📄 src/jobs/sync-products.ts                                   │
│  Function: syncProductsJob()                                    │
│  Line: 348-356                                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  const duration = ((Date.now() - startTime) / 1000).toFixed(2)  │
│                                                                 │
│  console.log("🎉 Product sync completed!")                     │
│  console.log(`✅ Created: ${createdCount}`)                     │
│  console.log(`🔄 Updated: ${updatedCount}`)                     │
│  console.log(`❌ Errors: ${errorCount}`)                        │
│  console.log(`⏱️  Duration: ${duration}s`)                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 File Structure Summary

```
medusa-store/
├── src/
│   ├── jobs/
│   │   └── sync-products.ts ─────────────┐
│   │       • syncProductsJob()           │ Main Entry Point
│   │       • fetchAllProducts()          │ Line 153
│   │       • mapToMedusaFormat()         │
│   │                                     │
│   ├── lib/                              │
│   │   ├── retry.ts ──────────────────┐  │
│   │   │   • fetchWithRetry<T>()      │  │ Utility Functions
│   │   │     Line 26-70               │  │
│   │   │                              │  │
│   │   └── batch-processor.ts ────────┤  │
│   │       • batchGenerator<T>()      │  │
│   │         Line 19-26               │  │
│   │                                  │  │
│   └── workflows/                     │  │
│       └── sync-categories.ts ────────┤  │
│           • syncCategoriesWorkflow   │  │ Workflow
│             Line 103-109             │  │
│           • syncCategoriesStep       │  │
│             Line 22-100              │  │
│                                      │  │
└── External Dependencies              │  │
    └── @medusajs/medusa/core-flows ───┤  │
        • createProductsWorkflow       │  │ Built-in Workflows
        • updateProductsWorkflow       │  │
        • createProductCategoriesWorkflow │
```

---

## 🔢 Function Call Order (Numbered)

```
1. MEDUSA CRON SCHEDULER
   └─> Triggers at midnight

2. src/jobs/sync-products.ts → syncProductsJob(container)
    Line: 153

3. src/jobs/sync-products.ts → fetchAllProducts()
    Line: 64
    │
    └─> 4. src/lib/retry.ts → fetchWithRetry<DummyJSONResponse>(url)
         Line: 26
         │
         └─> 5. Native fetch(url)
              Returns: DummyJSONResponse

6. src/workflows/sync-categories.ts → syncCategoriesWorkflow.run()
    Line: 103
    │
    └─> 7. src/workflows/sync-categories.ts → syncCategoriesStep()
         Line: 22
         │
         └─> 8. container.resolve("query").graph()
              │
              └─> 9. @medusajs/medusa/core-flows → createProductCategoriesWorkflow.run()
                   Returns: Created categories

10. src/jobs/sync-products.ts → container.resolve("query").graph()
     Line: 206
     Returns: Existing products

11. src/jobs/sync-products.ts → mapToMedusaFormat(product)
     Line: 102
     Returns: MedusaProductInput

12. src/lib/batch-processor.ts → batchGenerator(products, 15)
     Line: 19
     Yields: Batches of 15 products

13. @medusajs/medusa/core-flows → createProductsWorkflow.run()
     Creates products in database

14. @medusajs/medusa/core-flows → updateProductsWorkflow.run()
     Updates products in database

15. src/jobs/sync-products.ts → Log final summary
     Line: 348
```

---

## 🎯 Quick Reference: Where to Find Each Function

| Function | File | Line | Purpose |
|----------|------|------|---------|
| `syncProductsJob()` | `src/jobs/sync-products.ts` | 153 | Main entry point |
| `fetchAllProducts()` | `src/jobs/sync-products.ts` | 64 | Fetch from API with pagination |
| `fetchWithRetry()` | `src/lib/retry.ts` | 26 | Retry with exponential backoff |
| `mapToMedusaFormat()` | `src/jobs/sync-products.ts` | 102 | Transform data format |
| `batchGenerator()` | `src/lib/batch-processor.ts` | 19 | Generate batches |
| `syncCategoriesWorkflow` | `src/workflows/sync-categories.ts` | 103 | Category sync workflow |
| `syncCategoriesStep` | `src/workflows/sync-categories.ts` | 22 | Category sync step |

---

## 📞 External API Calls

| Call | Location | Line | Purpose |
|------|----------|------|---------|
| `fetch(url)` | `src/lib/retry.ts` | 35 | HTTP request to DummyJSON |
| `query.graph()` | `src/workflows/sync-categories.ts` | 28 | Query Medusa database |
| `query.graph()` | `src/jobs/sync-products.ts` | 206 | Query Medusa database |
| `createProductCategoriesWorkflow()` | `src/workflows/sync-categories.ts` | 47 | Medusa built-in workflow |
| `createProductsWorkflow()` | `src/jobs/sync-products.ts` | 307 | Medusa built-in workflow |
| `updateProductsWorkflow()` | `src/jobs/sync-products.ts` | 331 | Medusa built-in workflow |

---

This flow diagram shows the exact execution path with file locations and line numbers for every function call in your product sync job!
